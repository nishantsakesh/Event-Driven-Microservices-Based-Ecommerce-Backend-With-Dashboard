import { Separator } from "../ui/separator";

export default function AppSeparator(props) {
    return (
        <Separator
            className="bg-white/10"
            {...props}
        />
    );
}